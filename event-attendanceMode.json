{
  "title": "event.attendanceMode",
  "type": "string",
  "description": "Indicates if the [event](./event.json) is happening online, offline, or both (mixed).",
  "enum": [
    "online",
    "offline",
    "mixed"
  ]
}
