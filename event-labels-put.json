{
  "type": "object",
  "title": "event.labels",
  "description": "A list of labels that can be used to categorize the [event](./models/event.json).",
  "properties": {
    "labels": {
      "$ref": "./common-labels-optional.json"
    }
  },
  "required": [
    "labels"
  ]
}